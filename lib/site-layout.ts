/** Shared max width for header, hero, and page sections (Tailwind `max-w-7xl` = 80rem). */
export const siteMaxWidthClass = "max-w-7xl";

export const siteContainerClass = `mx-auto w-full ${siteMaxWidthClass} px-4 lg:px-12`;
